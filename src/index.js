import React from 'react';
import ReactDOM from 'react-dom';
import Rotas from './Rotas'; // Importe o componente Rotas

ReactDOM.render(
  <React.StrictMode>
    <Rotas /> {/* Renderize o componente Rotas */}
  </React.StrictMode>,
  document.getElementById('root')
);
